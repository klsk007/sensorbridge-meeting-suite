# SensorBridge Drivers

This camera-only workspace keeps only virtual camera driver/provider work.

Camera paths:

- `drivers/camera`: Media Foundation virtual camera prototype helpers.
- `drivers/camera/directshow`: DirectShow virtual camera provider and sender helpers for `SensorBridge Camera`.

The product path is WebRTC decoded video into the Windows virtual camera. Removed non-camera driver paths are not part of this workspace.
